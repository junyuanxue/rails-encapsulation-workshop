# Refactoring/Encapsulation in Rails

Install the repo:

* bundle
* rake db:setup

## Exercises

1. Refactor the articles_controller so it's better encapsulated
1. Change the logic so word count is calculated when you create the article
